spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/fitbuddy-users
    username: sarthaksolanki
    password: sarthak

  jpa:
    hibernate:
      ddl-auto: update
    datasource-platform: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8081